[build-system]
requires = ["setuptools>=68", "wheel", "setuptools-scm>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "apache-airflow-provider-cian"
dynamic = ["version"]
description = "Apache Airflow provider for Cian.ru Builder API — collect calls and chats statistics"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "Michael Kozhin", email = "michael@kozhin.cc"}]
keywords = ["airflow", "cian", "provider", "real-estate", "builder-api"]
classifiers = [
    "Framework :: Apache Airflow",
    "Framework :: Apache Airflow :: Provider",
    "Development Status :: 4 - Beta",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Internet :: WWW/HTTP",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]
dependencies = [
    "apache-airflow>=2.9.1,<3.0",
    "requests>=2.28",
]

[project.optional-dependencies]
dev = ["pytest"]

[project.urls]
Homepage = "https://github.com/mkozhin/airflow-provider-cian"
Documentation = "https://github.com/mkozhin/airflow-provider-cian#readme"
Repository = "https://github.com/mkozhin/airflow-provider-cian"
Changelog = "https://github.com/mkozhin/airflow-provider-cian/blob/main/CHANGELOG.md"
Issues = "https://github.com/mkozhin/airflow-provider-cian/issues"

[project.entry-points."apache_airflow_provider"]
provider_info = "airflow_provider_cian:get_provider_info"

[tool.setuptools_scm]
version_file = "airflow_provider_cian/_version.py"

[tool.setuptools.packages.find]
where = ["."]
include = ["airflow_provider_cian*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
